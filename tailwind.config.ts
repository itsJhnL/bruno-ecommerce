/**
 * DEPRECATED — this project uses Tailwind CSS v4, which is configured in CSS.
 *
 * All design tokens live in the `@theme` block at the top of `app/globals.css`
 * (Memory.md D-002). Tailwind v4 only reads this file if a stylesheet opts in
 * with an `@config` directive, and none does — so editing it has no effect.
 *
 * It is kept only so a stale v3 config left over from an earlier scaffold
 * cannot mislead you. Safe to delete.
 */
export {};
